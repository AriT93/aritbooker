%p
  = @user.email


- @fbs.me.home.data.each do |h|
  %p #{h.from.name} #{h.message}

%fb:user-status{:uid => fb[:user], :linked => true}

= pp(@fbs.me.home)



%a{:href => "#{@oauth_url}"}
  Link to Facebook
